# PostgreSQL database connection settings
DB_HOST=postgres-db
DB_TEST_HOST=postgres-db
DB_PORT=5432
DB_USER=meecha
DB_PASSWORD=meecha_pass
DB_NAME=meecha

DATABASE_URL="host=${DB_HOST} user=${DB_USER} password=${DB_PASSWORD} dbname=${DB_NAME} port=${DB_PORT} sslmode=disable TimeZone=Asia/Tokyo"

